Value Required local_pair ([A-D])
Value remote_pair ([A-D])
Value distance (\d+)
Value status (\w+)

Start
  ^.+Pair ${local_pair}\s+${distance}\s+\+\/- \d+ meters Pair ${remote_pair}\s+${status} -> Record