Value Required local_pair ([A-D])
Value Required status (\w+)
Value remote_pair ([A-D])
Value distance (\S+)
Value speed (\d+)
Value interface (#INTERFACE_PATTERN#)

Start
  ^(?:${interface}\s+${speed}[M|G])?\s+Pair ${local_pair}\s+${distance}\s+(?:Pair ?${remote_pair}\s+)?${status} -> Record